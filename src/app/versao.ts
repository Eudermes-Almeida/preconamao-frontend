// Mostrada em letra miúda no header (ver CabecalhoComponent), só para o usuário confirmar que o
// celular já carregou a versão mais nova depois de um deploy. Incrementar a cada push para a main.
export const VERSAO_APP = 'v1.5.6';
